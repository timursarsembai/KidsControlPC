# Сборка панели против своего сервера (данные остаются в РК).
# Firebase-переменные здесь намеренно не заданы: этот режим их не использует.
VITE_BACKEND=selfhosted
VITE_API_BASE_URL=https://api.kidscontrol.kz
