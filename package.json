{
  "name": "kids-control-pc",
  "version": "1.1.26",
  "description": "Parental control desktop application",
  "main": "out/main/index.js",
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "web:dev": "npm run dev --workspace=web",
    "web:dev:staging": "npm run dev:staging --workspace=web",
    "web:build": "npm run build --workspace=web",
    "web:build:staging": "npm run build:staging --workspace=web",
    "web:preview:staging": "npm run preview:staging --workspace=web",
    "web:deploy": "npm run deploy --workspace=web",
    "web:deploy:staging": "npm run build:staging --workspace=web && firebase deploy --only hosting --project dev",
    "dev": "electron-vite dev",
    "dev:staging": "electron-vite dev --mode staging",
    "build": "electron-vite build",
    "build:staging": "electron-vite build --mode staging",
    "build:win": "electron-vite build && electron-builder --win --x64",
    "agent:dev:staging": "npm run dev:staging --prefix agent",
    "agent:build": "npm run build --prefix agent",
    "agent:build:staging": "npm run build:staging --prefix agent",
    "preview": "electron-vite preview",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx"
  },
  "build": {
    "appId": "com.kidscontrol.pc",
    "productName": "KidsControlPC",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "displayLanguageSelector": true,
      "installerLanguages": [
        "en_US",
        "ru_RU"
      ]
    },
    "publish": [
      {
        "provider": "github",
        "owner": "timursarsembai",
        "repo": "KidsControlPC"
      }
    ]
  },
  "dependencies": {
    "@emoji-mart/data": "^1.2.1",
    "@emoji-mart/react": "^1.1.1",
    "electron-updater": "^6.8.3",
    "firebase": "^10.12.0",
    "i18next": "^26.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-i18next": "^17.0.8",
    "uuid": "^14.0.1",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "electron": "^31.1.0",
    "electron-builder": "^26.8.1",
    "electron-vite": "^2.3.0",
    "vite": "^5.3.1"
  }
}
